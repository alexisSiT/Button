/*import 'package:flutter/material.dart';

// const String _customText = "Este es un mensaje de que has enviado una alerta";

const List<String> _textCustom = [
  "Mensaje de demostración",
  "Mensaje de alerta",
  "Haciendo llamada",
  "Reportando"
];

class AppDialog {
  final int selectedText;

  AppDialog({this.selectedText = 0})
      : assert(selectedText >= 0, 'la opción debe de estar entre 0 y 3');

  AlertDialog aler()
}
*/
